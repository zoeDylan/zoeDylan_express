module.exports = function (config) {
    
    //配置文件缓存及更改
    config = (global.config?(config?config.cover?true:false:false)?require('../lib/options.js')(config, global.config, function (op) {
        global.config = op;
    }):global.config:(function () { global.config = config; return config; })());
    
    return {
        log: require('./lib/log.js')()
    };
}